# === Base principal (vintelarg_base @ Contabo :5433 via Tailscale) ===
# Red, campañas, auth, CRM. La usa todo el portal (Drizzle `db`).
DATABASE_URL=postgresql://pgadmin:PASSWORD@100.106.74.8:5433/vintelarg_base

# === Base analytics (aware_analytics :5434) — facturación real ===
# Segunda conexión Drizzle (`analyticsDb`): 04_facturacion_documentos_*.
# Las queries de ventas cruzan ambas bases vía la cascada (lib/ventas-cascada.ts).
ANALYTICS_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5434/aware_analytics

# === OIDC client (IdP = vintelarg-auth apps/idp) ===
AUTH_ISSUER=http://localhost:3000
AUTH_OIDC_BASE=http://localhost:3000/api/auth
AUTH_CLIENT_ID=
AUTH_CLIENT_SECRET=
AUTH_REDIRECT_URI=http://localhost:3002/api/auth/callback

# === App ===
APP_URL=http://localhost:3002
NODE_ENV=development

# === Secret de sesión (generá uno con: openssl rand -base64 32) ===
SESSION_SECRET=
