import { Proximamente } from "@/components/portal/proximamente";

// Destino genérico de los ítems-app no disponibles del menú (getNavForUser).
export default function ProximamentePage() {
  return <Proximamente />;
}
