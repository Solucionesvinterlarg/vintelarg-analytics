import { EmpAcademiaView } from "@/features/emprendedora/emp-academia-view";

// Academia = app externa (ingreso, sin badge MOCK). Ver ROLE_HREF_OVERRIDES.
export default function Page() {
  return <EmpAcademiaView />;
}
