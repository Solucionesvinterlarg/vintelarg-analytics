import { EstadoCuentaView } from "@/features/gerente/estado-cuenta-view";

// Perfil Gerente Comercial — datos MOCK (Lote 2) hasta definir KPIs.
export default function EstadoCuentaPage() {
  return <EstadoCuentaView />;
}
