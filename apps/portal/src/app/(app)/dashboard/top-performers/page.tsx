import { TopPerformersView } from "@/features/gerente/top-performers-view";

// Perfil Gerente Comercial — datos MOCK (Lote 2) hasta definir KPIs.
export default function TopPerformersPage() {
  return <TopPerformersView />;
}
